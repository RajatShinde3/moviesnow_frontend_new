"use client";

// Bridge page: mount the MFA settings experience under the protected settings route.
// Reuses the existing implementation.

export { default } from "@/app/(public)/mfa-enable-verify-disable/page";

